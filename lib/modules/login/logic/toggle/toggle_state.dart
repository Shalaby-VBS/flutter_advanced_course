part of 'toggle_cubit.dart';

class ToggleObscureTextState {
  final bool value;

  const ToggleObscureTextState(this.value);
}

class ToggleRememberMeState {
  final bool value;

  const ToggleRememberMeState(this.value);
}